export type FeatureType = {
  title: string;
  subtitle: string;
  image: string;
  points: string[];
};

export type FooterLinkItem = {
  title: string;
  path: string;
};

export type HeaderLinkItem = {
  path: string;
  title: string;
};
